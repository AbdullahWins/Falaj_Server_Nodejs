Author: https://github.com/abdullahwins
Base URL: https://thefalaj.onrender.com

//admin

router.get("/admins/find/:id", authenticateToken, getOneAdmin);
router.get("/admins/delete/:id", authenticateToken, deleteAdminById);
router.get("/admins", authenticateToken, getAllAdmins);
router.get("/admins/types/:typeName", authenticateToken, getAdminsByType);
router.post("/admins/add", authenticateToken, addOneAdmin); = { firstName, lastName, email, password, permissions, status }
router.post("/admins/register", RegisterAdmin); = { firstName, lastName, email, password, permissions, status }
router.post("/admins/login", LoginAdmin);
router.patch("/admins/edit/:id", authenticateToken, updateAdminById);
router.post("/admins/reset", sendPasswordResetLink); = { email }
router.patch("/admins/reset", updateAdminPasswordByEmail); = { email, newPassword }
router.patch("/admins/resetpassword/:email", updateAdminPasswordByOldPassword); = { oldPassword, newPassword }
router.delete("/admins/delete/:id", deleteAdminById); {_id}

//stripe

router.get("/stripe", getAllWhitelistedCustomers);
router.get("/stripe/find/:email", checkWhitelistedCustomersByEmail);
router.post("/stripe/paymentsuccessful",express.raw({ type: "application/json" }),handleStripeWebhookOnSuccess);

//users

router.get("/users/find/:id", getOneUser);
router.get("/users/types/:typeName", getUsersByType);
router.get("/users", getAllUsers);
router.post("/users/add", addOneUser); = { firstName, lastName, email, password }
router.patch("/users/edit/:id", updateUserById);
router.post("/users/reset", sendPasswordResetLink); = { email }
router.patch("/users/reset", updateUserPasswordByEmail); = { email, newPassword }
router.patch("/users/resetpassword/:email", updateUserPasswordByOldPassword); = { oldPassword, newPassword }
router.post("/users/login", LoginUser);
router.post("/users/register", RegisterUser); = { firstName, lastName, email, password }
router.get("/users/delete/:id", deleteUserById);
